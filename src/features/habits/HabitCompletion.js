// import React from 'react'
// import { selectDate} from '../features/points/pointsSlice'
// import { useSelector } from 'react-redux'
// import { useGetHabitPointsQuery } from '../api/apiSlice'

// const HabitCompletion = () => {

//     const selectPointsForUser = useMemo(() => {
//     return createSelector(
//       (res) => res.data,
//       (res, habitId) => habitId,
//         (data, habitId) => data.filter((habit) => habit.habitId === habitId) ?? []

//   return (
//     <div>HabitCompletion</div>
//   )
// }

// export default HabitCompletion
